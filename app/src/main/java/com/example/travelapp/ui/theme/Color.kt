package com.example.travelapp.ui.theme

import androidx.compose.ui.graphics.Color

val Pink80 = Color(0xFFEFB8C8)

// --- 앱 시그니처 컬러 ---
val PointRed = Color(0xFFFF5252)      // 📍 시그니처 레드 (핀, 버튼 등)
val Beige = Color(0xFFFFFFD5)         // 배경용 베이지
val CardWhite = Color(0xFFFFFFFF) // 카드는 하얀색으로 띄워서 입체감을 줍니다.
val bottomColor = Color(0xFFF2F1FF)

// --- 텍스트 컬러 시스템 (폴드4 가독성 최적화) ---
val TextMain = Color(0xFF000000)
val TextSub = Color(0xFF000000)
val TextGhost = Color(0xFF888888)   // 🌫️ 부가 설명, 날짜, 태그 (중간 회색)
val TextDisabled = Color(0xFFBDBDBD) // ⚪ 비활성화 문구, 약관 (연한 회색)

// --- 기타 ---
val DividerGray = Color(0xFFEEEEEE)  // 경계선용

